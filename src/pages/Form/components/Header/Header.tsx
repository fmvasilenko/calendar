import HeaderComponent from '../../../../shared/view/components/Header/Header';
import { Root } from './Header.style';

const Header = (): JSX.Element => {
  return (
    <Root>
      <HeaderComponent />
    </Root>
  );
};

export default Header;
