import { ToolType } from '../../../core/tool/tool.types';

interface Props {
  toolType: ToolType,
  label: string,
}

interface StyleProps {
  toolType: ToolType,
}

export default Props;
export type {
  StyleProps,
};
