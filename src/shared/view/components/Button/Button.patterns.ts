import { StylePatterns } from './Button.types';

const Patterns: StylePatterns = {
  resolve: {
    color: 'white',
    background: 'green',
  },
  reject: {
    color: 'red',
    background: 'white',
    border: 'red',
  },
};

export default Patterns;
